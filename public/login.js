import {
  inputEnabled,
  setDiv,
  token,
  message,
  enableInput,
  setToken,
} from "./index";

let loginDiv = null;
let email = null;
let password = null;

export const handleLogin = () => {};

export const showLogin = () => {};
